{
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable ./flutter && export PATH=\"$PWD/flutter/bin:$PATH\" && flutter config --enable-web && cd example && flutter pub get && flutter build web --release --no-tree-shake-icons",
  "outputDirectory": "example/build/web"
}
